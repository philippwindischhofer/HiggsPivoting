#!/bin/bash
# keep track of the root path for the codebase
export ROOTDIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" >/dev/null 2>&1 && pwd )"
#export HISTFITTER_ROOTDIR="PATH_TO_HISTFITTER_INSTALLATION_DIR"
#export PYTHONPATH="PATH_TO_ROOT_BUILD_DIR/lib":$PYTHONPATH
